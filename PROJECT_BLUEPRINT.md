# PROJECT_BLUEPRINT.md — HWPX 양식 기반 내용 채움 서비스

## 현재 작업 경로
- `C:/MABC/HWPX-STYLE-TRANSPLANT`
- 현재 브랜치: `team/engine` (P01 착수 상태)

## 서비스 목표
A는 HWPX 양식 파일이고, B는 HWPX·TXT·Markdown 또는 붙여넣은 내용이다.  
A의 **표, 문단, 서식, 고정 문구**를 그대로 보존한 채, B에서 필요한 내용만 찾아 입력란에 채우는 서비스를 만든다.

## A와 B의 역할
- **A = 양식 원본(HWPX)**  
  - 문서 골격: 표, 문단, 고정 문구, 페이지 구성, 서식 프로필  
  - 서식과 레이아웃의 출처이며, 이 서비스의 “틀” 역할
- **B = 내용 원본(HWPX / TXT / Markdown / 붙여넣기)**  
  - 채워 넣을 텍스트, 표 값, 제목, 문장 등 실제 내용  
  - B의 서식은 이번 서비스에서 신경 쓰지 않음. 필요한 내용만 추출해 A의 빈칸/입력 위치에 배치하는 데만 사용

## 결과물
- A를 기반으로 한 새 HWPX 문서  
  - A의 표·문단·서식·고정 문구가 유지됨  
  - B의 내용 중 서비스 로직이 판단한 항목만 A의 지정된 입력 위치에 채워짐  
- 결과물은 “A처럼 보이고, B가 일부 채워진” 문서가 목표

## 반드시 보존할 것
- A의 표 구조와 위치
- A의 문단 구성(고정 문구 포함)
- A의 서식(글꼴·크기·색·줄간격·여백·표 서식은 양식 기준)
- A에 미리 박혀 있는 고정 문구
- 결과물이 A의 틀에서 벗어나지 않는 것

## 이번 서비스에서 적용하지 않는 것
- 예선의 “A 문장 제거” 규칙은 적용하지 않는다.  
- 예선의 “B 구조 그대로 유지” 규칙을 이번 기능에 적용하지 않는다.  
- 지금은 A가 틀, B가 채우는 내용이라는 관점에서만 설계한다.

## 설계 상태
- 이 문서는 이번 단계의 설계 문서다.  
- 코드, 스크립트, 변환 로직, Git 변경, 다른 문서는 이 단계에서 만들지 않는다.  
- 다음 프롬프트를 받아 이어서 진행한다.

## 전체 처리 흐름
브라우저에서 사용자가 A 양식 파일과 B 내용(HWPX·TXT·Markdown·붙여넣기)을 제출한다.  
넘겨받는 것은 A 파일, B 원본, 그리고 B가 파일인지 붙여넣기인지 같은 출처 정보 정도다.  
이 진입점에서는 아직 분석이나 채움을 하지 않고, 서버로 보낼 준비를 한다.

서버는 A를 받아 **A의 입력 후보**를 분석한다.  
입력은 A 파일이고, 넘기는 것은 A의 표·문단·입력 위치 후보 목록, 각 후보의 위치와 종류·서식 정보, 고정 문구와 편집 가능 구간의 구분이다.  
이 단계의 목적은 “어디를 채울 수 있는지”를 찾는 것이지, 실제로 내용을 채우는 것이 아니다.

서버는 B를 받아 **B의 근거**를 분석한다.  
입력은 B 원본이고, 넘기는 것은 B에서 추출한 텍스트·표 값·문장 조각, 그리고 각 조각이 어떤 형태에서 왔는지에 대한 근거 정보다.  
B의 서식은 이번 서비스의 판단 재료가 아니며, 내용 조각과 그 출처 단서만 남긴다.

서버는 A의 입력 후보와 B의 근거를 연결해 **명확한 값부터 규칙으로** 채운다.  
입력은 A의 입력 후보와 B의 근거이고, 넘기는 것은 규칙 기반으로 확정된 채워짐 후보, 즉 값·근거·적용 위치가 함께 붙은 결과다.  
이 단계는 “이 값은 근거가 분명하다”는 것부터 처리한다.

키가 있으면 Solar가 **남은 항목을 자동으로 제안**한다.  
입력은 규칙으로 연결되지 않은 A의 남은 입력 후보, B의 근거, 그리고 이 시점까지 쌓인 채움 정보다.  
넘기는 것은 Solar가 각 남은 항목에 대해 제안한 값, 제안 이유, 신뢰도 표시다.  
제안은 확정 값이 아니라 검토 대상이고, 사용자가 그대로 쓸지 선택할 수 있어야 한다.

서버는 **근거를 검사**한다.  
입력은 규칙 기반 채움 결과, Solar 제안 값, 각 값의 근거이고, 넘기는 것은 근거 검사 결과다.  
여기에는 어떤 값이 어떤 근거로 연결됐는지, 근거가 부족하거나 충돌하는지, 검토가 필요한 항목이 무엇인지 포함된다.  
이 단계는 생성 전에 값이 어디서 왔는지 다시 보는 관문 역할을 한다.

사용자가 검토한 뒤 **적용할 칸을 선택**해 생성을 요청한다.  
입력은 사용자가 선택한 채움 항목, 즉 적용하거나 적용하지 않기로 결정한 항목들이고, 넘기는 것은 그 선택만 반영한 채움 결정 목록이다.  
사용자가 원하지 않은 항목은 결과에 반영되지 않는다.

생성할 때 서버는 **A와 B의 해시와 근거를 다시 확인**한다.  
입력은 A의 현재 상태(해시 포함), B의 현재 상태(해시 포함), 선택 반영 목록, 저장할 근거 정보다.  
넘기는 것은 재검증 결과, 재생성에 필요한 입력 상태 확인, 그리고 적용할 구간 결정이다.  
이 단계는 처음 본 A와 B 그대로인지 다시 보는 단계다.

서버는 **A의 선택한 구간만 편집**해 결과를 만든다.  
입력은 A 원본, 선택한 구간과 채워질 값, 근거 정보이고, 넘기는 것은 A의 선택된 부분만 바뀌고 나머지는 유지된 편집본이다.  
A가 가진 표·문단·서식·고정 문구는 이 단계에서 지키는 것을 전제로 한다.

결과 생성 후 서버는 **다시 검증**한다.  
입력은 편집된 결과 문서, A의 선택 구간과 채움 정보이고, 넘기는 것은 검증 결과다.  
여기에는 변경된 구간이 의도한 범위인지, A의 보존 항목이 유지됐는지, 문제가 있는지 여부가 포함된다.  
이 단계는 내려보내기 전에 결과가 설계와 어긋나지 않았는지 보는 마지막 확인이다.

검증이 끝나면 **HWPX와 보고서를 내려받는다**.  
입력은 검증된 결과 문서, 근거와 검토 정보, 요청한 범위이고, 넘기는 것은 최종 HWPX 파일과 근거·검토 흐름이 드러나는 보고서다.  
이 단계가 사용자가 받는 결과물이다.

## 문서 엔진의 역할
 서비스는 Python으로 구현한다. 문서 엔진은 아래 파일들로 구성하며, 각 파일은 한 가지 책임만 맡는다.

- `hwpx/analyze.py` — A 분석  
  A 양식 파일을 읽고 입력 후보, 표·문단 구조, 고정 문구, 편집 가능 구간을 추출한다.  
  A가 가진 서식 정보는 이 단계에서 함께 정리하지만, 실제 채우기는 하지 않는다.

### A 분석 조건
`hwpx/analyze.py`는 A의 모든 section을 문서 순서로 읽는다.  
이때 namespace, 병합 범위, 중첩 표를 구분해 처리한다.  
같은 텍스트가 부모 셀과 자식 셀에 함께 나타나면 두 번 세지 않도록 중복 계상을 막는다.

찾아야 할 후보 유형은 빈 셀, 라벨 뒤 공백, 단위만 남은 칸, 자리표시자다.  
글이 여러 run에 나뉘어 있어도 문단 기준으로 분석해 하나의 문장·하나의 단위로 다룬다.  
직명과 성명, 주소와 우편번호, 시간과 분은 별도 구간으로 나눠 본다.  
표 제목과 구역, 다단 헤더, 연도, 좌우 영역, 단위 범위는 구분 정보로 유지한다.

안내문과 기존 기입값은 보호한다.  
안전한 문단은 수동 보정 후보로 남겨 둔다.  
이 분석의 결과는 실제 채움이 아니라, 어디를 건드릴 수 있는지와 각 구간의 성격을 함께 남기는 것을 목표로 한다.

- `hwpx/source.py` — B 추출  
  B 원본(HWPX·TXT·Markdown·붙여넣기)에서 내용 조각과 근거 정보를 추출한다.  
  B의 서식은 다루지 않고, 원문과 문맥을 보존한 상태로 채움에 쓸 텍스트·표 값·문장 조각과 그 출처 단서만 남긴다.

### B 추출 조건
`hwpx/source.py`는 **원문과 표시용 정규화 값을 구분**해 유지한다.  
같은 내용이라도 화면에 보이는 형태와 내부 처리에 쓰는 형태를 섞어 쓰지 않는다.  
외부에 보이는 값을 그대로 근거처럼 다루지 말고, 원문과 정규화 값을 따로 남겨야 한다.

추출할 때는 **원문과 문맥 보존**을 우선한다.  
제목 계층, 표의 행·열, 반복 행 식별자는 보존한다.  
긴 블록은 UTF-8 기준 6000 바이트 안에서 나누되, 순서대로 합치면 원문과 같아야 한다.  
나누는 과정에서 의미나 순서가 바뀌지 않도록 하고, 재결합했을 때 원문과 동일해야 한다.

표 내용과 단위 변환은 `hwpx/rules.py`가 맡는다.  
`hwpx/source.py`는 표 연결이나 단위 변환을 직접 책임지지 않는다.

### 규칙 연결 조건
`hwpx/rules.py`는 A의 입력 후보와 B의 근거를 연결할 때 **명확성이 높은 것부터** 처리한다.  
연결 근거가 분명한 값부터 확정하고, 애매한 항목은 뒤로 남긴다.

표 연결에서는 **위치나 열 개수만 기준으로 삼지 않는다.**  
제목과 구역, 열 의미, 고정 행 이름처럼 내용을 식별할 수 있는 요소를 함께 본다.  
상위 금액을 하위 행마다 복사하지 말고, 소계와 상세를 구분해 연결한다.  
단위 변환은 Decimal로 정확히 처리해 오차를 남기지 않는다.

여러 사실을 조합해 하나의 값을 만드는 경우에는 **모든 근거를 남기고 검토를 요구**한다.  
근거가 하나로 압축되지 않은 조합 결과는 확정 값으로 쓰지 않는다.  
자료 없음, 충돌, 분석 실패는 서로 구분해 다룬다.  
없음인지, 서로 다른 근거가 부딪힌 것인지, 추출이나 연결이 실패한 것인지 따로 남겨야 한다.

- `hwpx/rules.py` — 규칙 연결  
  A의 입력 후보와 B의 근거를 받아 명확한 값부터 규칙으로 연결한다.  
  표 연결과 단위 변환은 이 파일이 맡는다.  
  어떤 후보가 어떤 근거로 채워질 수 있는지 결정하고, 연결된 결과를 넘긴다.

- `hwpx/evidence.py` — 원문 근거 검사  
  연결된 값과 제안 값의 근거를 검사한다.  
  근거가 충분한지, 충돌하는지, 검토가 필요한 항목을 가려낸다.

- `hwpx/corrections.py` — 입력란 보정  
  서버가 만든 안전한 candidateId의 라벨과 활성화 여부만 보정한다.  
  값 수정은 사용자 편집 목록에서 처리하고, 클라이언트가 새 좌표를 지정하지 못하게 한다.  
  여러 근거의 조합은 모든 근거를 검증한 뒤 사용자 확인을 거쳐 적용할 수 있다.  
  하나의 근거로 압축해야 한다는 조건은 두지 않는다.

- `hwpx/generate.py` — 결과 생성  
  원본 A의 허용 구간만 편집해 결과를 만든다.  
  실제 편집 위치는 서버 내부에 두고, 화면에는 후보 ID와 구조 위치만 보여 준다.

### 생성 조건
`generate.py`는 원본 A에서 **편집 위치를 다시 계산**해 결과를 만든다.  
문단 전체 편집과 내부 입력란이 겹치면 그 요청은 거부한다.  
빈 값과 미선택은 A를 그대로 유지하고, 수동 입력은 원문에서 확인된 값과 구분해 남긴다.

긴 내용은 자르거나 글꼴을 줄이지 않고 **넘침 경고**만 남긴다.  
변경 문단의 linesegarray, 무효해진 Preview, 정확한 참조만 캐시 정리를 허용한다.  
고정 문구, 표와 병합, 스타일과 이미지는 보호한다.

결과를 다시 읽어 **기입값을 비교**하고, 합계와 반복값 불일치는 경고로 남긴다.  
구조 손상은 파일 생성 실패로 처리한다.

- `hwpx/validate.py` — 결과 검증  
  생성된 결과의 구조와 기입값을 검사한다.  
  A의 보존 항목이 유지됐는지, 편집이 의도한 구간에만 적용됐는지 확인한다.

이번 단계에서는 파일 경로와 책임만 적고, 함수나 빈 코드 파일은 만들지 않는다.

## 서비스 구조

### Solar
- `solar/client.py` — 모델의 단일 요청과 응답 검사  
  Solar에 단일 요청을 보내고, 받은 응답의 형식과 내용을 검사한다.  
  Solar 응답은 값, 필드 ID, 원문 블록 ID, 인용문, 검토 필요 여부와 이유를 담는다.  
  모델의 신뢰도 표시만으로 확정하지 않는다.  
  내용 연결 제안만 다루며, XML 생성이나 바이트 수준의 편집은 Solar에 맡기지 않는다.  
  정상적인 내용 증가에 따라 생기는 페이지 이동과, 보호 구조가 손상된 경우를 구분해 본다.

- `solar/batches.py` — 입력란과 관련 원문을 배치로 나누는 역할  
  입력란 후보와 관련 원문 조각을 묶어 배치 요청을 구성한다.  
  Solar에는 내용 연결 판단만 맡기고, 문서 구조를 만들거나 파일을 편집하는 역할은 넘기지 않는다.

### API
- `api/service.py` — dispatch  
  상태, 분석, 제안, 생성, 미리보기를 하나의 dispatch로 처리한다.  
  각 단계의 진입과 결과를 이 dispatch에서 연결한다.

- `api/index.py` — 배포 연결  
  같은 dispatch를 배포 환경에서 노출한다.  
  요청 라우팅과 HTTP 경계를 여기서 연결한다.

- `scripts/dev_server.py` — 로컬 실행 연결  
  같은 dispatch를 로컬 개발 서버에서 실행한다.  
  배포와 로컬 실행이 같은 dispatch를 공유하도록 연결한다.

### Web
- `web`은 HTML과 CSS, JavaScript로 화면을 구현한다.  
  업로드, 검토, 다운로드를 제공하는 웹 인터페이스다.  
  브라우저가 A와 B를 보유하고, 요청마다 필요한 원본만 서버로 보낸다.  
  서버에 문서 세션을 저장하지 않는다.

## 화면과 실행 예산

### 화면
 화면은 표 셀과 입력란 목록을 연결해 보여준다.  
 각 항목에는 라벨, 값, 근거, 충돌 후보를 함께 보여준다.  
 값만 나열하지 말고, 그 값이 어디서 왔는지와 충돌이 있는지도 함께 볼 수 있어야 한다.

 보정 설정에는 A 해시와 후보 설정만 저장한다.  
 결과 구조는 결과 파일에서 다시 만든다.  
 근사 화면을 실제 한글 조판과 같다고 표시하지 않는다.

 입력을 변경하면 이전 응답과 다운로드를 무효화한다.  
 같은 요청이라도 입력이나 선택이 바뀌면 이전 결과를 그대로 쓰지 않는다.

목차

- 문서 엔진의 역할  
- 서비스 구조  
- 화면과 실행 예산  
- 완료 기준  
- 다음 작업 목적

서비스 구조의 주요 구성은 이미 적었다. 핵심 점검만 남기면 다음과 같다.

- `solar/client.py`: 단일 요청·응답, Solar 응답은 값·필드 ID·원문 블록 ID·인용문·검토 필요 여부와 이유를 담고, 신뢰도만으로 확정하지 않는다.
- `solar/batches.py`: 입력란과 관련 원문을 배치로 나눈다.
- `api/service.py`: 상태·analysis·제안·생성·미리보기를 하나의 dispatch로 처리한다.
- `api/index.py`: 같은 dispatch를 배포에서 노출한다.
- `scripts/dev_server.py`: 같은 dispatch를 로컬에서 실행한다.
- `web`: HTML·CSS·JavaScript로 업로드·검토·다운로드를 제공하고, 서버에는 문서 세션을 저장하지 않는다.

전송 예산은 다음과 같이 적는다.

- HTTP 요청과 응답은 각각 3,800,000 bytes 이하다.
- 디코딩한 A와 B의 합계, 출력 ZIP은 각각 2,500,000 bytes 이하다.
- ZIP 해제는 파일당 50,000,000 bytes, 엔트리 5,000개까지다.
- 보고서와 미리보기도 응답 크기에 포함한다.

두 사람의 역할 분리는 다음과 같다.

- 담당자 1 Halley04: `team/service`에서 Solar, API, 화면, 배포를 맡는다.
- 담당자 2 wayne0525: `team/engine`에서 엔진, 공통 계약, 준비 검사, main 통합을 맡는다.
- 두 사람은 별도 clone을 사용하고, 같은 준비 커밋에서 구현을 시작한다.

입력란 보정 범위는 `hwpx/corrections.py`가 맡으며, 서버가 만든 안전한 candidateId의 라벨과 활성화 여부만 보정한다. 값 수정은 사용자 편집 목록에서 처리하고, 클라이언트가 새 좌표를 지정하지 못하게 한다. 여러 근거 조합은 모든 근거를 검증한 뒤 사용자 확인을 거쳐 적용할 수 있으며, 하나의 근거로 압축해야 한다는 조건은 두지 않는다.

### 실행 예산
 모델은 solar-pro4를 쓴다.  
 서버 키는 UPSTAGE_API_KEY를 사용한다.  
 키 존재와 실제 연결 성공은 구분해 다룬다.  
 키가 있는지와 실제로 요청이 성공했는지는 같은 것으로 보지 않는다.

 한 배치 요청은 20개 이하로 보낸다.  
 모델 요청은 48000 bytes 이하로 제한한다.  
 동시 요청은 2개까지 허용한다.  
 전체 240개 제한은 두지 않는다.

## 완료 기준

### 평가 구성
 완료 기준은 3~5개의 실제 업무 양식과 별도 평가 양식으로 확인한다.  
 각 양식에는 사람이 정한 입력 위치와 정답이 있어야 한다.  
 사람이 정한 위치와 정답이 없는 문서는 이 단계의 완료 기준으로 쓰지 않는다.

### 지표와 목표
탐지 precision과 recall은 양식별로 100%를 목표로 한다.  
근거 있는 필수값의 정확 기입률도 양식별로 100%를 목표로 한다.  
오기입, 필수 누락, 구조 손상은 0이어야 한다.  
실제 검사가 없는 상태는 통과로 적지 않는다.  
숫자 목표는 낮게 잡아 통과시키는 방식이 아니라, 양식별로 그대로 적용한다.

### 결과 기록
 자동 결과와 수동 수정 후 결과를 따로 기록한다.  
 자동 상태에서 어떤 항목이 채워졌는지, 수동 수정 뒤에 무엇이 달라졌는지를 분리해 남긴다.  
 두 결과를 섞어 하나로 포장하지 않는다.

### 통과 조건
 0개 실행과 skip은 통과가 아니다.  
 live, 브라우저, 실제 한글, 배포는 실제 검사로 확인한다.  
 속도는 같은 환경에서 준비 2회 후 최소 10회 측정한다.  
 빈 값만 반환하거나 품질을 낮춰 통과하지 않는다.

### 문서의 위치
 이 문서는 설계 문서다.  
 구현과 성능이 검증됐다고 기록하지 않는다.  
 기준이 여기에 적혀 있다고 해서 현재 실행 결과가 그 기준을 충족했다는 뜻은 아니다.

## 다음 번호
- P02: (미정 — 전체 처리 흐름에 맞춰 다음 준비 항목을 정의)

## 다음 작업 목적
다음 작업은 루트 `SKILL.md`를 이번 양식 채우기 목표에 맞게 정리하는 것이다.  
이번 양식 채우기 서비스는 A가 틀, B가 채우는 내용이라는 관점에서 설계했으며, 예선의 A 문장 제거와 B 구조 유지 규칙은 적용하지 않는다.  
루트 SKILL.md에는 이 방향을 기준으로 HWPX 양식 기반 내용 채움 서비스의 사용 방식과 판단 원칙을 정리한다.  
그때까지는 PROJECT_BLUEPRINT.md와 루트 SKILL.md 사이의 충돌 정리를 먼저 끝내는 것을 목적으로 한다.
